import PropTypes from 'prop-types'

/**
 * User: { id, username, name, avatar, homepage, apiDetail }
 * Release: { id, name, title, abstract, body, draft, prerelease, created_at, published_at, author: User }
 * Source: Name() string, Type() string, Setup({}) error, List({since=null}) []Release, Latest() Release, IsLatest(name) bool
 */

const __sources = {}

const m = () => {

}

export default m